# Optional - if set, WeatherProvider would call a real API instead of the synthetic one.
# Left blank in Phase 1: SyntheticWeatherProvider is always used regardless of this value.
WEATHER_API_KEY=

# CORS: origins allowed to call this service directly (normally only the Spring Boot backend).
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:5173
